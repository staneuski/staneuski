# sops creation rules. The age key below is your PUBLIC key — safe to commit.
# The matching PRIVATE key lives at /var/lib/sops-nix/key.txt on each host and
# must NEVER be committed.
keys:
  - &user age12h90sken5f8qawny4uw85wxj6e5rxd6cg98zfmvsc86l0tsj6vfqrdn97m
creation_rules:
  - path_regex: secrets/[^/]+\.yaml$
    key_groups:
      - age:
          - *user
